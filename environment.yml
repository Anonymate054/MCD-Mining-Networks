# conda env create --file environment.yml
# mamba env create --file environment.yml
# conda env export --from-history > environment.yml

name: DMN
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - black
  - fs
  - invoke
  - ipykernel
  - ipython
  - jupyter
  - jupyterlab
  - matplotlib
  - nbdime
  - numpy
  - pandas
  - pip
  - pyjanitor
  - pylint
  - pyprojroot
  - python-dotenv
  - python=3.11
  - seaborn
  - sqlalchemy
  - pyodbc
prefix: /home/lenovo/anaconda3/envs/DMN
